backend:
  name: git-gateway
  branch: main

media_folder: "static/img"
public_folder: "/static/img"

collections:
  # ========================================
  # UPLOAD MULTIPLE
  # ========================================
  - name: "upload-multiple"
    label: "📸 Upload Multiple"
    label_singular: "Upload Multiple"
    files:
      - name: "upload-tool"
        label: "Outil d'Upload Multiple"
        file: "admin/upload-tool.md"
        fields:
          - { name: title, label: "Titre", widget: "hidden", default: "Upload Multiple" }
          - { name: description, label: "Description", widget: "markdown", default: "## 📸 Upload Multiple\n\nUtilisez cet outil pour uploader plusieurs photos à la fois avec des titres automatiques.\n\n### Comment utiliser :\n1. Cliquez sur le bouton ci-dessous\n2. Sélectionnez vos photos\n3. Choisissez la catégorie et le nom d'album\n4. L'upload se fait automatiquement\n\n### Avantages :\n- ✅ Upload en lot\n- ✅ Titres automatiques\n- ✅ Organisation par album\n- ✅ Génération d'index automatique" }
          - { name: upload_button, label: "Lancer l'Upload Multiple", widget: "string", default: "📸 Ouvrir l'outil d'upload multiple" }

  # ========================================
  # PORTRAITS
  # ========================================
  - name: "portrait"
    label: "📸 Portraits"
    label_singular: "Portrait"
    folder: "content/portfolio/portrait"
    create: true
    slug: "{{album}}/{{slug}}"
    path: "{{album}}/{{slug}}"
    media_folder: "/static/img/portrait/{{album}}"
    public_folder: "/static/img/portrait/{{album}}"
    fields:
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Titre", name: "title", widget: "string", hint: "Ex: Portrait Marie 1, Portrait Marie 2..." }
      - { label: "Album", name: "album", widget: "string", hint: "Nom du shooting (ex: Marie, Jean, Famille Dupont). Tape le MÊME nom pour grouper plusieurs photos ensemble !" }
      - { label: "Date", name: "date", widget: "datetime", default: "" }
      - { label: "Catégorie", name: "category", widget: "hidden", default: "Portrait" }

  # ========================================
  # MARIAGES
  # ========================================
  - name: "mariage"
    label: "💍 Mariages"
    label_singular: "Mariage"
    folder: "content/portfolio/mariage"
    create: true
    slug: "{{album}}/{{slug}}"
    path: "{{album}}/{{slug}}"
    media_folder: "/static/img/mariage/{{album}}"
    public_folder: "/static/img/mariage/{{album}}"
    fields:
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Titre", name: "title", widget: "string", hint: "Ex: Sophie & Marc 1, Sophie & Marc 2..." }
      - { label: "Album", name: "album", widget: "string", hint: "Nom du mariage (ex: Sophie & Marc). Tape le MÊME nom pour grouper plusieurs photos ensemble !" }
      - { label: "Date", name: "date", widget: "datetime", default: "" }
      - { label: "Catégorie", name: "category", widget: "hidden", default: "Mariage" }

  # ========================================
  # IMMOBILIER
  # ========================================
  - name: "immobilier"
    label: "🏠 Immobilier"
    label_singular: "Immobilier"
    folder: "content/portfolio/immobilier"
    create: true
    slug: "{{album}}/{{slug}}"
    path: "{{album}}/{{slug}}"
    media_folder: "/static/img/immobilier/{{album}}"
    public_folder: "/static/img/immobilier/{{album}}"
    fields:
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Titre", name: "title", widget: "string", hint: "Ex: Villa Bordeaux 1, Villa Bordeaux 2..." }
      - { label: "Album", name: "album", widget: "string", hint: "Nom du bien (ex: Villa Bordeaux). Tape le MÊME nom pour grouper plusieurs photos ensemble !" }
      - { label: "Date", name: "date", widget: "datetime", default: "" }
      - { label: "Catégorie", name: "category", widget: "hidden", default: "Immobilier" }

  # ========================================
  # PAYSAGES
  # ========================================
  - name: "paysage"
    label: "🌄 Paysages"
    label_singular: "Paysage"
    folder: "content/portfolio/paysage"
    create: true
    slug: "{{album}}/{{slug}}"
    path: "{{album}}/{{slug}}"
    media_folder: "/static/img/paysage/{{album}}"
    public_folder: "/static/img/paysage/{{album}}"
    fields:
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Titre", name: "title", widget: "string", hint: "Ex: Bretagne 1, Bretagne 2..." }
      - { label: "Album", name: "album", widget: "string", hint: "Nom du lieu (ex: Bretagne). Tape le MÊME nom pour grouper plusieurs photos ensemble !" }
      - { label: "Date", name: "date", widget: "datetime", default: "" }
      - { label: "Catégorie", name: "category", widget: "hidden", default: "Paysage" }

  # ========================================
  # MACRO
  # ========================================
  - name: "macro"
    label: "🔬 Macro"
    label_singular: "Macro"
    folder: "content/portfolio/macro"
    create: true
    slug: "{{album}}/{{slug}}"
    path: "{{album}}/{{slug}}"
    media_folder: "/static/img/macro/{{album}}"
    public_folder: "/static/img/macro/{{album}}"
    fields:
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Titre", name: "title", widget: "string", hint: "Ex: Insectes 1, Insectes 2..." }
      - { label: "Album", name: "album", widget: "string", hint: "Nom du thème (ex: Insectes). Tape le MÊME nom pour grouper plusieurs photos ensemble !" }
      - { label: "Date", name: "date", widget: "datetime", default: "" }
      - { label: "Catégorie", name: "category", widget: "hidden", default: "Macro" }

  # ========================================
  # LIFESTYLE
  # ========================================
  - name: "lifestyle"
    label: "✨ Lifestyle"
    label_singular: "Lifestyle"
    folder: "content/portfolio/lifestyle"
    create: true
    slug: "{{album}}/{{slug}}"
    path: "{{album}}/{{slug}}"
    media_folder: "/static/img/lifestyle/{{album}}"
    public_folder: "/static/img/lifestyle/{{album}}"
    fields:
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Titre", name: "title", widget: "string", hint: "Ex: Urban Style 1, Urban Style 2..." }
      - { label: "Album", name: "album", widget: "string", hint: "Nom du shooting (ex: Urban Style). Tape le MÊME nom pour grouper plusieurs photos ensemble !" }
      - { label: "Date", name: "date", widget: "datetime", default: "" }
      - { label: "Catégorie", name: "category", widget: "hidden", default: "Lifestyle" }
